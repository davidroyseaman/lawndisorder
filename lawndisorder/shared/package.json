{
  "name": "shared",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "dsx_nothing.js",
  "scripts": {
    "clean": "rm tsconfig.tsbuildinfo; rm -rf ./dist",
    "warn": "echo \"You shouldn't need to run this manually\"",
    "build": "npm run warn; tsc --build",
    "dev": "npm run warn; tsc --build --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "prettier": "^3.0.0",
    "typescript": "^7.0.2"
  }
}
